# GPU install-of-record:
#   pip install -r requirements.txt
#
# This file's sole job is to install the project via the PyTorch CUDA wheel
# index (where torch / torchvision +cuXXX wheels live; they aren't on PyPI).
# Version constraints live in pyproject.toml -- this file just adds the index
# URL and points pip at the project directory. Pip resolves every other dep
# (lightning, torchmetrics, etc.) against pyproject's abstract floors.
#
# For a CPU/default install use `pip install .` straight from pyproject.toml.
--extra-index-url https://download.pytorch.org/whl/cu130
.
